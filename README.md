# Lemonade 🍹

Школьный дневник — это гениальное изобретение! Им было удобно пользоваться в школе, некоторые продолжают им пользоваться
в вузе, а мы решили создать электронную версию такого дневника для обычной жизни. Наш дневник сочетает в себе
ностальгические нотки школьного прошлого, а так же простоту и гениальность расписания всё в одном и концепцию «неделя на
одном листе».

## Концепция

За основу взят разворот бумажного школьного дневника:

- Слева — понедельник, вторник, среда.
- Справа — четверг, пятница, а ниже — суббота и воскресенье: визуально одна общая клетка (как в бумажном дневнике),
  но фактически две независимые клетки меньшей высоты, каждая под свой день.

```
┌───────────┬───────────┐
│ Пн        │ Чт        │
├───────────┼───────────┤
│ Вт        │ Пт        │
├───────────┼───────────┤
│           │ Сб        │
│ Ср        ├───────────┤
│           │ Вс        │
└───────────┴───────────┘
```

Каждый день — не набор отдельных строк-полей, а единое разлинованное текстовое поле с переносами, куда пользователь
пишет дела в свободной форме с лёгкой markdown-разметкой (списки, выделения и т.п.), которая сразу рендерится
отформатированной — пользователь не видит «сырой» markdown-синтаксис. В превью будничной клетки видно первые ~6 строк
дня, а в клетках Сб и Вс — по ~3 строки каждая (клетка вдвое ниже); строки не переносятся — если строка не помещается
по ширине клетки, она обрезается, а не переносится на следующую. Клик по клетке дня открывает его в отдельном окне
для просмотра и редактирования всего содержимого целиком.

Сверху страницы — текущие год, месяц и номер недели; текущий день в сетке визуально выделен. Переключаться между
неделями (в прошлое и в будущее) можно двумя способами: свайпом «неделя назад / неделя вперёд», либо кликом по
году/месяцу — по клику открывается список недель для перехода к произвольной.

## ТЗ (MVP)

### Модель данных

- **Неделя** — определяется датой понедельника (началом недели), явно как сущность не хранится, а вычисляется.
- **День** — `(userId, date)` → одно текстовое поле с содержимым в markdown. Один день = один документ, без деления
  на подстроки/задачи — переносы строк и разметка внутри поля свободные, парсер их не структурирует.
- Сб и Вс — это два разных дня с двумя разными датами и двумя независимыми клетками в сетке, отдельными от будних
  дней только уменьшенной высотой превью.

### Пользователи и аутентификация

- **Пользователь** — `name`, `email` (уникальный, служит логином), `password` (хеш), `timezone`, секретный вопрос +
  ответ (для восстановления пароля). Фамилия и прочие поля не нужны — дневник полностью личный, никому кроме
  владельца не показывается.
- Регистрация: обязательны `name`, `email`, `password`, `timezone` (поле предзаполняется автоопределением на
  клиенте, пользователь может его поправить вручную), секретный вопрос из предложенного списка + ответ на него.
- Авторизация: `email` + `password`.
- Восстановление пароля: без email-рассылки — пользователь вводит `email`, отвечает на свой секретный вопрос и,
  если ответ верный, задаёт новый пароль. Верификация email на этапе MVP не нужна.
- Список секретных вопросов — контент, который нужно будет придумать отдельно (не часть этого ТЗ).
- Сама аутентификация (регистрация/вход/выход/восстановление) реализуется через Inertia — обычные серверные роуты
  и контроллеры с сессией (`web`-guard), без выдачи токена на руки пользователю. Sanctum нужен не для этого, а для
  API-запросов — либо от внешних потребителей, либо для внутренних AJAX-вызовов самого приложения в процессе
  работы (например, сохранение дня без полной перезагрузки страницы) поверх той же сессии.
- Вход запоминается всегда, отдельной галочки «запомнить меня» в форме нет: дневник личный, ставится как PWA и
  должен открываться сразу рабочим.
- **Сессии независимы по устройствам.** Штатный механизм Laravel хранит один токен на пользователя
  (`users.remember_token`), из-за чего выход на телефоне разлогинивал заодно и компьютер. Вместо него — таблица
  `remember_tokens`, по строке на каждый вход (хранится sha256 токена, не он сам). Соответственно:
  - выход гасит только то устройство, с которого вышли;
  - смена пароля в профиле гасит все остальные устройства, но не текущее — иначе человек выбрасывал бы сам себя
    сразу после смены пароля;
  - восстановление пароля по секретному вопросу гасит все устройства без исключения: пароль там меняет тот, кто
    в аккаунт ещё не вошёл, и исходить надо из того, что доступ мог быть потерян.
  - Срок жизни записи — год, скользящий: каждое восстановление сессии из cookie его продлевает, поэтому
    устройство, которым пользуются, не разлогинится, а забытое отвалится само.
- Экрана «Мои устройства» пока нет — он в разделе «Запланировано после MVP» ниже; таблица хранит `user_agent` и
  `last_used_at` именно под него.

Пометки по реализации, чтобы не выяснять заново:

- **Ротации токена при каждом использовании нет, и это осознанно.** Перевыпуск токена на каждом восстановлении
  сессии — следующий уровень защиты: он позволяет заметить кражу cookie (пришли со старым токеном — значит его
  кто-то скопировал). Но перевыпуск требует переписать cookie в ответе, а провайдер пользователей доступа к ответу
  не имеет — понадобился бы собственный guard вместо подмены двух методов. Для дневника, где нет ни денег, ни
  чужих данных, цена и риск такой замены выше пользы.
- **Повторный вход на устройстве, где ещё жив старый cookie, оставляет одну недостижимую строку** — cookie
  перезаписывается новым, а старая запись висит до истечения срока. Войти по ней нельзя (её значения больше нигде
  нет), на безопасность не влияет, отдельного кода не заслуживает. Случается редко: обычно человек с валидным
  cookie до формы входа просто не доходит, его редиректит на `/now`.
- **Смена пароля не перевыпускает recaller-cookie текущего устройства.** В cookie лежит старый хеш пароля, но
  `SessionGuard` его не проверяет — сегмент нужен только middleware `AuthenticateSession`, которого в стеке нет.
  Если он когда-нибудь появится, это место придётся пересмотреть.

### Страницы

MVP состоит из шести базовых страниц — все реализованы как Inertia-компоненты (`resources/js/Pages/*.vue`),
рендерятся из соответствующих контроллеров на `web`-роутах:

| Роут | Доступ | Назначение |
| --- | --- | --- |
| `/` | публичный | Домашняя — посадочный лендинг с питчем продукта (см. вступление README) и переходом на регистрацию/вход. |
| `/register` | публичный (авторизованного редиректит на `/now`) | Регистрация — форма из раздела «Пользователи и аутентификация» выше. |
| `/login` | публичный (авторизованного редиректит на `/now`) | Авторизация — `email` + `password`, ссылки на `/register` и на восстановление пароля. |
| `/forgot-password` | публичный (авторизованного редиректит на `/now`) | Восстановление пароля — один компонент с локальным состоянием на три шага: email → секретный вопрос → новый пароль. Отдельный роут (а не часть `/login`), чтобы на него можно было перейти напрямую по ссылке. |
| `/now` | только авторизованным (редирект на `/login`) | Календарь — основной экран приложения, сетка недели; подробности ниже в «Экран «Неделя»». |
| `/me` | только авторизованным (редирект на `/login`) | Профиль — просмотр и редактирование своих данных; подробности ниже в «Профиль». |

`/now` и `/me` — единственные страницы за авторизацией, у них общий элемент интерфейса (шапка/навигация) со
ссылкой на профиль и кнопкой выхода.

### Экран «Неделя» (`/now`)

- Сетка 2×3: слева Пн/Вт/Ср (равной высоты), справа Чт/Пт/Сб/Вс, где Сб и Вс — две клетки вдвое ниже обычных.
- Каждая клетка — превью дня: заголовок (день недели + дата) и первые строки содержимого без скролла внутри клетки
  (~6 строк для Пн–Пт, ~3 строки для Сб и Вс).
- Текущий день визуально выделен в сетке.
- Шапка страницы показывает год, месяц и номер текущей отображаемой недели. Номер недели отсчитывается от начала
  месяца (а не как ISO-номер недели года); неделя, разбитая между двумя месяцами, целиком принадлежит тому месяцу,
  в котором больше её дней.
- Переключение недель: свайп «вперёд/назад» на одну неделю; в дополнение к свайпу — кнопки «вперёд/назад» рядом с
  шапкой (см. «Адаптивность» ниже: видимость кнопок зависит от размера экрана). Клик по году/месяцу в шапке
  открывает пикер недель — мини-календарь, где вместо дней ячейки в формате `[число–число]`, каждая ячейка — целая
  неделя, клик по ней переключает на неё. «Текущая неделя» — состояние по умолчанию при открытии.

### Редактирование дня

- Клик по клетке дня открывает модальное окно/отдельный экран с полным содержимым этого дня.
- В окне — единое разлинованное текстовое поле (визуально имитирует тетрадный лист), где markdown-разметка
  рендерится сразу по ходу ввода (WYSIWYG, без отображения «сырого» синтаксиса); переносы строк свободные, без
  ограничения по числу строк — лимит на количество видимых строк действует только в превью на сетке недели.
- Автосохранение по мере ввода или явная кнопка «Сохранить» — конкретика на усмотрение реализации.
- Просмотр и редактирование используют один и тот же компонент (нет отдельного read-only режима).

### Профиль (`/me`)

- Просмотр и редактирование собственных данных пользователя: `name`, `email`, `timezone`, секретный вопрос/ответ
  (можно сменить), смена пароля (требует ввода текущего пароля).
- Кнопка выхода — завершает сессию, редиректит на `/`.
- Дополнительных полей (аватар, настройки и т.п.) в MVP нет — только то, что уже есть в модели пользователя.

### Адаптивность

- Приложение должно быть одинаково удобно на компьютере, планшете и телефоне — не просто «не ломаться», а не
  мешать пользоваться основным сценарием (сетка недели, редактирование дня) на любом из них.
- Свайп «вперёд/назад» по неделе — основной способ навигации на touch-устройствах, но не единственный: кнопки
  «вперёд/назад» дублируют его для обратной совместимости (мышь/трекпад, доступность, устройства без touch).
- Видимость кнопок зависит от размера экрана: на телефоне (ширина вьюпорта < 768px) кнопки скрыты — экран тесный,
  свайп остаётся единственным видимым способом; на планшете и десктопе (≥ 768px) кнопки видны всегда, независимо от
  того, touch это устройство или нет. Брейкпоинт — по ширине, а не по типу указателя (`pointer: coarse/fine`),
  потому что у планшетов тоже touch-экран, но места на кнопки уже достаточно.

### Offline и PWA

- Приложение должно устанавливаться на смартфон и открываться как отдельное приложение (`manifest.json`,
  `display: standalone`, «Добавить на экран»), а не только как сайт в браузере.
- Работа должна продолжаться и без сети: последняя загруженная неделя и её дни остаются доступны для просмотра и
  редактирования офлайн за счёт локального хранилища на устройстве (IndexedDB), а не только оперативной памяти
  вкладки.
- Правки, сделанные офлайн, помечаются как несинхронизированные и хранятся локально до восстановления связи; при
  восстановлении связи они автоматически отправляются на сервер без участия пользователя.
- Конфликт (день отредактирован офлайн на одном устройстве и параллельно изменён — например, с другого устройства —
  пока первое было офлайн) разрешается по принципу «последняя правка побеждает»: сравниваются времена правок,
  более поздняя сохраняется. Явного UI разрешения конфликтов в MVP нет.
- Кэширование статики приложения (JS/CSS/шрифты) для офлайн-запуска — через service worker.

### Вне рамок MVP

- Совместное использование дневника несколькими пользователями, шаринг недель.
- Напоминания/уведомления.
- Явный UI для ручного разрешения конфликтов синхронизации (конфликты разрешаются автоматически, см. «Offline и
  PWA» выше).

### Запланировано после MVP

Не блокирует выпуск, но решено делать — в отличие от раздела выше, где перечислено то, чего не будет.

- **Экран «Мои устройства».** Серверная часть уже готова: таблица `remember_tokens` хранит по строке на устройство
  вместе с `user_agent` и `last_used_at` (см. «Пользователи и аутентификация»), а `App\Auth\DeviceTokens` умеет
  гасить как отдельную запись, так и все сразу. Не хватает только интерфейса:
  - список активных устройств — узнаваемое название (разобранный `user_agent`, а не сырая строка), когда
    пользовались последний раз, и явная пометка «это устройство» (текущее определяется через
    `DeviceTokens::presented()`, который достаёт токен из recaller-cookie);
  - кнопка «выйти» у каждой записи и общая «выйти на всех остальных»;
  - место — отдельный роут (например, `/me/devices`) либо секция на `/me`, решается при вёрстке.

## Сущности

Персистентных сущностей в приложении две — `User` и `Day`; всё остальное («Неделя», «Месяц») — производные
значения, вычисляемые из `date`, а не отдельные таблицы.

### User

| Поле | Тип | Обязательное | Примечание |
| --- | --- | --- | --- |
| `id` | integer | — | первичный ключ |
| `name` | string | да | только имя, без фамилии |
| `email` | string | да | уникальный, служит логином |
| `password` | string (hash) | да | хешируется как обычно |
| `timezone` | string | да | IANA-идентификатор (`Europe/Moscow` и т.п.); автоопределяется на клиенте при регистрации, пользователь может поправить вручную; валидируется по реальному списку зон, а не произвольная строка |
| `secret_question` | string | да | один из предложенного набора вопросов (контент набора — вне этого ТЗ) |
| `secret_answer` | string (hash) | да | ответ на секретный вопрос, хешируется так же, как пароль — не хранится в открытом виде |
| `created_at` / `updated_at` | timestamp | — | стандартные Laravel-таймстемпы |

Один `User` — владелец произвольного количества `Day`; связи с другими пользователями нет (шаринга нет, см. «Вне
рамок MVP»).

### Day

| Поле | Тип | Обязательное | Примечание |
| --- | --- | --- | --- |
| `id` | integer | — | первичный ключ |
| `user_id` | integer (FK → `users.id`) | да | владелец дня |
| `date` | date | да | календарная дата дня (не день недели — суббота и воскресенье это два разных `Day` с разными `date`) |
| `content` | text | нет | markdown-текст дня; пустой/`null`, пока пользователь ничего не написал — клетка при этом всё равно рендерится, просто пустая |
| `created_at` / `updated_at` | timestamp | — | стандартные Laravel-таймстемпы |

Уникальный индекс — `(user_id, date)`: на пару пользователь+дата не может быть больше одной записи. История версий
не хранится (см. «Вне рамок MVP») — обновление `content` просто перезаписывает текущее значение.

### Производные понятия (не таблицы)

- **Неделя** — Пн–Вс, вычисляется от даты понедельника; на экране `/now` это семь `Day` (реальных или ещё не
  созданных) пользователя, лежащих в этом диапазоне дат.
- **Месяц/номер недели в шапке** — тоже вычисляется на лету по правилу «неделя принадлежит месяцу, в котором
  большинство её дней» (см. «Экран «Неделя»» выше), отдельно нигде не хранится.

## Технический стек

### Уже есть и работает

- **Backend**: Laravel 13 на PHP 8.4, `vendor/`, `artisan` и базовая структура приложения на месте.
- **Аутентификация**: сессионная, на стоковом `web`-guard — регистрация, вход, выход, восстановление пароля по
  секретному вопросу и профиль реализованы. `laravel/sanctum` нужен только для того, чтобы `auth:sanctum` на
  роутах из `routes/api.php` принимал сессию самого приложения; токены на руки никому не выдаются.
  `config/auth.php` опубликован: провайдер пользователей подменён на `eloquent-devices` ради независимых сессий
  по устройствам (см. «Пользователи и аутентификация»).
- **API-документация**: `dedoc/scramble` генерирует OpenAPI по роутам/валидации, отдаётся на `/docs/api`.
- **Frontend**: `inertiajs/inertia-laravel` + Vue 3, полностью собранный build-тулинг — `vite.config.js` (плагины
  `laravel-vite-plugin`, `@vitejs/plugin-vue`; алиасы `@` → `resources/js` и `@css` → `resources/css`, оба
  зеркалированы в `jsconfig.json`), npm-скрипты `dev`/`build`.
- **Стили**: Tailwind CSS 4 (`tailwindcss` + `@tailwindcss/vite`, CSS-first конфиг — `@import "tailwindcss";` в
  `resources/css/app.css`, подключён из `resources/js/app.js`; обычный `.css`, не `.scss` — Tailwind v4 не
  рекомендует смешивать себя с Sass-препроцессором). Базовая вёрстка под «тетрадный» стиль интерфейса — сетка
  недели на `/now`, карточки форм авторизации — уже написана, но это не финальный дизайн.
- **База данных**: MariaDB (не sqlite по умолчанию Laravel).
- **Кэш/очереди/сессии**: Redis для кэша и очередей; сессии — на `database`-драйвере.
- **Инфраструктура**: трёхстадийный `Dockerfile` (base-образ с PHP-расширениями → vendor → финальный), `compose.yml`
  с сервисами `app`/`mariadb`/`redis` и health-check-гейтингом, `entrypoint.sh` гоняет миграции и поднимает очередь/
  шедулер при каждом старте контейнера.
- **PWA и офлайн-режим для `/now`**: `vite-plugin-pwa` настроен под Laravel (ручная регистрация SW,
  манифест руками в `app.blade.php` — см. выше), манифест заполнен под проект. Поверх этого:
  - **Под `npm run dev` PWA не работает и работать не может.** Dev-версию service worker'а отдаёт сам Vite по
    адресу `/dev-sw.js?dev-sw`, а страницу здесь отдаёт Laravel со своего порта; регистрация резолвит путь
    относительно origin страницы, запрос уходит в Laravel и получает 404. Сослаться на порт Vite тоже нельзя —
    скрипт service worker обязан быть с того же origin, что и страница. Поэтому `devOptions.enabled: false`,
    регистрация в `app.js` обёрнута в `import.meta.env.PROD`, а `<link rel="manifest">` в `app.blade.php` —
    в `@unless (Vite::isRunningHot())`. Проверять PWA нужно на сборке: `npm run build`, затем обычный запуск.
  - Service worker кэширует именно `/now` (`runtimeCaching` в `vite.config.js`, стратегия `NetworkFirst`) — это
    единственная страница, которая обязана открываться офлайн (см. «Offline и PWA» выше); остальные роуты
    (`/login`, `/me` и т.п.) принципиально требуют сервера и офлайн не работают.
  - Локальное зеркало дней — IndexedDB через `idb` (`resources/js/offline/db.js`): и синхронизированные дни, и
    ещё не отправленные офлайн-правки (флаг `dirty`).
  - Схема IndexedDB описана миграциями — `resources/js/offline/migrations.js`: по одной миграции на версию,
    список только дополняется в конец, версия базы берётся из последней миграции (руками её нигде указывать не
    надо). Выпущенные миграции не правят — нужна правка, добавляется следующая. Имена store'ов внутри миграции
    пишутся литералами, чтобы переименование константы в `db.js` не меняло смысл старой миграции задним числом.
  - `resources/js/offline/sync.js` — фоновая синхронизация: сначала отправляет накопленные офлайн-правки
    (`POST /api/days/batch`), затем подтягивает чужие изменения (`GET /api/days/sync?since=`); запускается на
    старте приложения и по событию `online`.
  - Конфликты — «последняя правка побеждает» по заявленному клиентом времени правки, а не по времени получения
    запроса сервером (см. `DayController::batch`) — иначе долго добиравшаяся из офлайна правка могла бы
    неправомерно перезаписать более свежую.
  - `Pages/Now.vue`: при заходе офлайн подменяет пропсы (которые могли прийти из закэшированной, устаревшей
    HTML-страницы) на актуальное состояние из IndexedDB; правки дня пишутся в IndexedDB сразу и оптимистично, до
    попытки отправки на сервер.
  - **Переключение недель работает офлайн**: раньше `goToWeek()` делал Inertia-переход на `/now?date=`, то есть
    поход на сервер, и без сети листание просто не срабатывало. Теперь неделя переключается на месте — каркас
    (даты, дни недели, «сегодня») считается на клиенте в `resources/js/week.js` (порт `App\Support\WeekCalculator`,
    включая правило «неделя принадлежит месяцу с большинством её дней»), содержимое берётся из IndexedDB и, если
    есть сеть, обновляется из `GET /api/weeks/{date}`. Адрес правится через `history.pushState`, кнопки
    «назад/вперёд» браузера обрабатываются через `popstate`.
  - Даты на клиенте — `dayjs`. Плагины (`utc` → `timezone` → `isoWeek`, порядок важен: `timezone` построен на
    `utc`) подключаются ровно в одном месте — `resources/js/dayjs.js`, оттуда же импортируется сам `dayjs`:
    `extend()` мутирует модуль глобально, и разбросанные по файлам вызовы сделали бы работоспособность `.tz()`
    зависящей от порядка загрузки модулей. `lodash` импортируется по одной функции
    (`import keyBy from 'lodash/keyBy'`), а не целиком.
  - Правка дня сохраняется не только по уходу из клетки, но и по ходу набора — с паузой (`debounce`, 700 мс),
    чтобы не слать запрос на каждое нажатие. Иначе закрытая посреди записи вкладка уносила бы всё набранное.
  - Неделю, которую ни разу не выкачивали с сервера, офлайн нельзя показывать как пачку пустых дней — иначе
    правка поверх невидимого содержимого затрёт его при следующей синхронизации. Поэтому в IndexedDB есть
    отдельный store `weeks` с отметками «эта неделя загружена целиком»; без отметки неделя показывается как
    «нет данных» и не редактируется. Пока запрос к серверу в пути, неделя считается загруженной авансом —
    иначе каждый переход на новую неделю в онлайне моргал бы этой заглушкой.
  - Отдельный набор правил против «пружинящей» страницы и случайного зума на iOS/iPadOS — Safari отскакивает
    по вертикали на любом касании, даже когда прокручивать нечего, и увеличивает масштаб по двойному тапу;
    и то и другое сбивало свайп переключения недели:
    `touch-action: manipulation` на `body` (убирает двойной тап, панорама и щипковый зум остаются — через
    `user-scalable=no` в `<meta viewport>` это сделать нельзя, iOS Safari игнорирует его с iOS 10 именно
    чтобы страница не могла запретить масштабирование),
    `overscroll-behavior: none` на `html` (гасит отскок и pull-to-refresh, прокрутку как таковую не трогает),
    `overscroll-contain` на каждом прокручиваемом блоке, чтобы достижение его конца не «перетекало» в страницу,
    и `touch-action: pan-y pinch-zoom` на сетке недели — обработчики свайпа пассивные и отменить прокрутку не
    могут, поэтому горизонтальную панораму приходится запрещать декларативно. Плюс `min-h-dvh` вместо
    `min-h-screen` на страницах входа: `100vh` в iOS не учитывает адресную строку, из-за чего скроллилась даже
    форма, помещающаяся целиком.
  - Иконки — готовый экспорт иконки приложения в `public/favicon/` (Android-mipmap'ы + iOS-`AppIcon.appiconset`).
    В манифест и в `app.blade.php` подтянуты подходящие по размеру файлы из него: 192×192 —
    `android/mipmap-xxxhdpi/lemonade.png`, 512×512 — `android/ic_launcher-web.png`, maskable —
    `android/playstore-icon.png` (единственный непрозрачный во всю канву), apple-touch-icon —
    `ios/AppIcon.appiconset/Icon-App-60x60@3x.png`. Отдельного `favicon.ico` в наборе нет, поэтому иконки
    прописаны явными `<link rel="icon">` в `app.blade.php`.

### Чего не хватает для реализации MVP

**Бэкенд**

- **Санитизация markdown при рендере.** Содержимое дня сейчас хранится и показывается как обычный текст, разметка
  нигде не разбирается. Как только появится настоящий рендер (см. «Редактирование дня» в ТЗ), понадобится защита
  от XSS — список разрешённых тегов при выводе, а не доверие к тому, что прислал клиент.
- **Тестов нет вообще.** В `composer.json` отсутствует секция `require-dev`: ни PHPUnit/Pest, ни каталога
  `tests/`, ни `phpunit.xml` — то есть нет и команды, которой что-либо можно прогнать. Первыми покрытия просят три
  места, где ошибка не видна глазом: `App\Support\WeekCalculator` вместе с его клиентским портом
  `resources/js/week.js` (две независимые реализации одного правила, обязаны совпадать до дня — особенно на стыке
  месяцев), разрешение конфликтов в `DayController::batch` и правила погашения токенов в `App\Auth\DeviceTokens`.
- **Фабрики и сидеры.** `User` подключает `HasFactory`, но `database/factories` пуст, а `database/seeders` ничем
  не наполнен — без них не заводятся ни тесты, ни ручная проверка на осмысленных данных.

**Frontend**

- **Модальный WYSIWYG-редактор дня.** Сейчас в клетке голый `<textarea>` с автосохранением: он проверяет механизм
  сохранения и офлайн-очереди, но это не тот UX, который описан в «Редактировании дня». Markdown-редактора в
  `package.json` нет ни одного, кандидата нужно выбирать отдельно (Tiptap с markdown-расширением, Milkdown и т.п.).
- **Выбор недели из шапки.** По ТЗ («Экран «Неделя»») заголовок с годом, месяцем и номером недели должен открывать
  календарь, у которого ячейки — диапазоны `[дата–дата]`, а не отдельные дни. Сейчас это неинтерактивный `<h1>`,
  и единственный способ попасть на далёкую неделю — листать по одной.
- **Брейкпоинт для кнопок переключения недели.** Свайп работает, кнопки-дублёры «назад/вперёд» есть, но
  показываются всегда. По ТЗ («Адаптивность») ниже 768px их быть не должно — на телефоне навигация только свайпом.
- **Дизайн-проход.** Вёрстка всех страниц написана в «тетрадном» стиле и рабочая, но это первая версия, а не
  финальный дизайн.

**Решено иначе, чем в ТЗ**

- Превью дня не обрезается по фиксированному числу строк (~6 для будних, ~3 для Сб/Вс). Вместо этого клетка
  прокручивается: видимую часть ограничивает её высота, остальное доступно скроллом. Горизонтально строка
  по-прежнему обрезается, а не переносится. Если решим вернуть исходное поведение — это `contentLines()`
  в `Pages/Now.vue`.

## Тестирование

Автоматических тестов в проекте нет (см. «Чего не хватает для реализации MVP» выше), поэтому проверка
пока полностью ручная. Чеклист сценариев — регистрация, вход, восстановление пароля, профиль и
независимость сессий по устройствам — в [`CHECKLIST.md`](CHECKLIST.md).

## Сборка

Сейчас можно собрать и опубликовать только базовый образ (`base`-стейдж `Dockerfile`) — он содержит PHP 8.4-FPM
на Alpine с нужными расширениями (redis, imagick, gd, intl, pdo_mysql и т.д.), composer и пользователя `www`.
Он публикуется в Docker Hub под тем же namespace, что уже используется в `Dockerfile` как основа для стейджей
`vendor` и финального (`vovikko/alpine-php-fpm`).

```bash
# Собрать образ из base-стейджа
docker build --target base -t vovikko/alpine-php-fpm:8.4 .

# Авторизоваться в Docker Hub (один раз)
docker login

# Опубликовать образ
docker push vovikko/alpine-php-fpm:8.4
```

Тег версии (`8.4`) должен совпадать с тем, что указан в `Dockerfile` в `FROM vovikko/alpine-php-fpm:8.4` для
стейджей `vendor` и финального — иначе последующая сборка приложения будет тянуть образ, не содержащий изменений
из `base`.
