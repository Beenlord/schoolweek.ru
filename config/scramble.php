<?php

use Dedoc\Scramble\Http\Middleware\RestrictedDocsAccess;

return [

    /*
    |--------------------------------------------------------------------------
    | Путь API
    |--------------------------------------------------------------------------
    |
    | По умолчанию в документацию попадают все маршруты, начинающиеся с этого
    | пути. Если нужно изменить это поведение, добавьте собственный резолвер
    | маршрутов через `Scramble::routes()`.
    |
    */

    'api_path' => 'api',

    /*
    |--------------------------------------------------------------------------
    | Домен API
    |--------------------------------------------------------------------------
    |
    | По умолчанию используется домен приложения. Это значение также является
    | частью стандартного матчера маршрутов API, поэтому при реализации
    | собственного матчера учитывайте этот параметр, если он необходим.
    |
    */

    'api_domain' => null,

    /*
    |--------------------------------------------------------------------------
    | Путь экспорта
    |--------------------------------------------------------------------------
    |
    | Путь, по которому будет экспортирована спецификация OpenAPI.
    |
    */

    'export_path' => 'api.json',

    /*
    |--------------------------------------------------------------------------
    | Информация об API
    |--------------------------------------------------------------------------
    |
    | Общие сведения об API, которые отображаются на главной странице
    | документации (`/docs/api`).
    |
    */

    'info' => [
        /*
         * Версия API.
         */
        'version' => env('API_VERSION', '0.0.1'),

        /*
         * Описание, отображаемое на главной странице документации API (`/docs/api`).
         */
        'description' => '',
    ],

    /*
    |--------------------------------------------------------------------------
    | Настройка интерфейса Stoplight Elements
    |--------------------------------------------------------------------------
    |
    | Здесь можно настроить внешний вид и поведение UI документации API,
    | построенного на библиотеке Stoplight Elements.
    |
    */

    'ui' => [
        /*
         * Заголовок сайта документации. Если оставить `null`, используется имя приложения.
         */
        'title' => 'Bigdata API',

        /*
         * Тема оформления документации. Доступные варианты: `light`, `dark`, `system`.
         */
        'theme' => 'system',

        /*
         * Скрыть возможность "Try It" (пробный запрос). По умолчанию включена.
         */
        'hide_try_it' => false,

        /*
         * Скрыть схемы в оглавлении. По умолчанию включены.
         */
        'hide_schemas' => false,

        /*
         * URL изображения, отображаемого как небольшой квадратный логотип рядом
         * с заголовком, над оглавлением.
         */
        'logo' => '',

        /*
         * Политика передачи учётных данных для функции "Try It".
         * Доступные варианты: omit, include (по умолчанию), same-origin.
         */
        'try_it_credentials_policy' => 'include',

        /*
         * Доступны три варианта раскладки Elements:
         * - sidebar - (по умолчанию) трёхколоночный дизайн с изменяемой по размеру боковой панелью.
         * - responsive - как sidebar, но на маленьких экранах боковая панель сворачивается в выдвижную панель.
         * - stacked - всё в одну колонку, удобно для встраивания в существующие сайты со своими колонками.
         */
        'layout' => 'responsive',
    ],

    /*
    |--------------------------------------------------------------------------
    | Серверы API
    |--------------------------------------------------------------------------
    |
    | Список серверов API. По умолчанию, когда указано `null`, URL сервера
    | формируется из параметров `scramble.api_path` и `scramble.api_domain`.
    | При передаче массива URL локального сервера нужно указать вручную
    | (если требуется).
    |
    | Пример нестандартной конфигурации (итоговые URL формируются с помощью
    | хелпера Laravel `url`):
    |
    | ```php
    | 'servers' => [
    |     'Live' => 'api',
    |     'Prod' => 'https://scramble.dedoc.co/api',
    | ],
    | ```
    |
    */

    'servers' => null,

    /*
    |--------------------------------------------------------------------------
    | Стратегия хранения описаний enum-кейсов
    |--------------------------------------------------------------------------
    |
    | Определяет, как Scramble хранит описания кейсов enum.
    | Доступные варианты:
    | - 'description' – описания кейсов хранятся в поле description схемы enum
    |                    в виде таблицы.
    | - 'extension'   – описания кейсов хранятся в расширении схемы enum
    |                    `x-enumDescriptions`.
    | - false          – описания кейсов игнорируются.
    |
    | @see https://redocly.com/docs-legacy/api-reference-docs/specification-extensions/x-enum-descriptions
    |
    */

    'enum_cases_description_strategy' => 'description',

    /*
    |--------------------------------------------------------------------------
    | Стратегия хранения имён enum-кейсов
    |--------------------------------------------------------------------------
    |
    | Определяет, как Scramble хранит имена кейсов enum.
    | Доступные варианты:
    | - 'names'    – имена кейсов хранятся в расширении схемы enum `x-enumNames`.
    | - 'varnames' – имена кейсов хранятся в расширении схемы enum `x-enum-varnames`.
    | - false       – имена кейсов не сохраняются.
    |
    */

    'enum_cases_names_strategy' => false,

    /*
    |--------------------------------------------------------------------------
    | Разворачивание вложенных query-параметров
    |--------------------------------------------------------------------------
    |
    | Когда Scramble встречает глубоко вложенные объекты в query-параметрах,
    | он разворачивает их в плоский вид, чтобы итоговый документ OpenAPI
    | корректно описывал API. Разворачивание актуально до выхода OpenAPI 3.2,
    | в которой структуру query-строки можно будет описывать напрямую.
    |
    | Например, правило валидации `['foo.bar' => ['required', 'int']]` описывает
    | объект со свойством `bar`.
    |
    | Если `flatten_deep_query_parameters` равно `true`, Scramble опишет параметр так:
    | `{"name":"foo[bar]", "schema":{"type":"int"}, "required":true}`.
    |
    | Если `flatten_deep_query_parameters` равно `false`, Scramble опишет параметр так:
    | `{"name":"foo", "schema": {"type":"object", "properties":{"bar":{"type": "int"}}, "required": ["bar"]}, "required":true}`.
    |
    */

    'flatten_deep_query_parameters' => true,

    /*
    |--------------------------------------------------------------------------
    | Middleware
    |--------------------------------------------------------------------------
    |
    | Middleware, через которые проходят маршруты документации и экспорта
    | спецификации API. `RestrictedDocsAccess` по умолчанию ограничивает
    | доступ к документации только локальным окружением.
    |
    */

    'middleware' => [
        'web',
        RestrictedDocsAccess::class,
    ],

    /*
    |--------------------------------------------------------------------------
    | Расширения
    |--------------------------------------------------------------------------
    |
    | Список расширений Scramble, используемых для дополнения анализа типов
    | и генерации документации сверх стандартного поведения пакета.
    |
    */

    'extensions' => [],

];